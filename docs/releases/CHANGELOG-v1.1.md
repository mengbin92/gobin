# Gobin v1.1.0 更新日志

## 发布日期 - 2026-05-10

Gobin v1.1.0 是一次向后兼容的演进版本，重点提升生成器可维护性、构建可观测性、开发体验和发布可靠性。

---

## 新增功能

### 增量构建与可观测性

- 页面渲染结果现在会统计 `rendered` 和 `skipped`。
- 页面内容未变化时会跳过重写输出文件，避免无意义的 mtime 变化。
- `build` 和 `serve` 输出会同时展示页面渲染统计和静态资源复制统计。

### 资源版本化

- 新增 `assetURL` 模板函数。
- 默认模板和官方主题资源会按内容 hash 自动追加 `?v=...`。
- 外部 CDN URL 和未知资源会保持原样，避免破坏用户自定义资源。

### 开发服务器 LiveReload

- `gobin serve` 新增开发期 LiveReload 能力。
- 默认在 watch 模式下启用，可通过 `--live-reload=false` 关闭。
- LiveReload 脚本只在开发服务器响应 HTML 时注入，不会写入生成目录。

### 发布产物

- 发布脚本会生成平台压缩包。
- Release 附件包含 `SHA256SUMS`，便于下载后校验。

---

## 改进

- 拆分生成器规划边界，降低页面、内容和 artifact 流程之间的耦合。
- 模板渲染错误会带上输出路径、页面标题和模板候选信息。
- 官方网站和发布文档中的兼容性表述更准确。
- 新增统一 `make lint` 入口，CI 使用同一套 lint 规则。
- 官方主题和示例站点展示版本更新到 `v1.1.0`。

---

## 兼容性

- 本版本保持配置和内容结构向后兼容。
- `assetURL` 是新增模板函数，不影响已有模板函数。
- LiveReload 仅在 `serve` 的开发服务器中生效，不改变 `gobin build` 的静态输出。

---

## 验证

发布前已执行：

```bash
go test ./...
make lint
git diff --check
```
